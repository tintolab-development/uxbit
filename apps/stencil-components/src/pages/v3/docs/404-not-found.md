# 화면 와이어프레임 (Text Wireframe)

화면ID: ERROR_404_004  
화면명: 404 오류 대체 UI

---

## 1) 화면 목적

- **404 오류 시 사용자 안내**: 잘못된 배너 ID 클릭 시 발생하는 오류를 시각적 메시지로 전달
- **복구 경로 제공**: 홈으로 돌아가기 버튼을 통해 사용자 흐름 복구
- **GA4 이벤트 기록**: `error_404` 이벤트로 오류 발생 횟수 측정

---

## 2) 레이아웃 구조 (Top → Bottom)

- **Header**: 없음
- **Body**:
  - 중앙 정렬된 "준비 중" 메시지 (폰트: 24px, 굵은체)
  - 하단에 "홈으로 돌아가기" 버튼 (44x44px 터치 영역 보장)
- **Footer**: 없음

---

## 3) 컴포넌트 상세

### 1) "준비 중" 메시지

- **표시 조건**:
  - 잘못된 배너 ID 클릭 시 발생하는 404 오류
- **사용자 액션**:
  - 메시지 클릭 시 무시 (인터랙션 제한)
- **시스템 응답**:
  - GA4 이벤트 `error_404` 자동 기록
  - 정적 컨텐츠 표시 (API `/api/error/404` 호출)
- **디자인 특징**:
  - 중앙 정렬, WebP 이미지 사용 시 배경 없음
  - 접근성: 텍스트 대비 색상 4.5:1 이상

### 2) "홈으로 돌아가기" 버튼

- **표시 조건**:
  - 404 오류 발생 시 항상 표시
- **사용자 액션**:
  - 버튼 클릭 시 `HOME_001` 화면으로 이동
- **시스템 응답**:
  - `GET /api/home` 호출
  - 터치 타겟 크기 44x44px 보장 (WCAG 2.1 준수)
- **디자인 특징**:
  - 둥근 모서리 (8px), 푸른색(#007BFF)

---

## 4) 인터랙션 플로우

1. **진입 경로**: `PROMO_BANNER_003` → 잘못된 ID 클릭 → 404 오류
2. **메인 액션**:
   - "홈으로 돌아가기" 버튼 클릭 → `HOME_001` 화면으로 이동
3. **복구 경로**:
   - 뒤로 가기 버튼 클릭 시 `PROMO_BANNER_003` 화면 유지 (캐시 사용 불가)

---

## 5) 상태

- **Loading**: 없음 (정적 컨텐츠)
- **Empty**: 없음 (메시지 자체가 콘텐츠)
- **Error**:
  - 404 오류 발생 시 GA4 이벤트 자동 기록
- **Success**:
  - 홈으로 돌아가기 성공 시 `HOME_001` 진입
- **Permission Denied**: 없음 (공용 오류 UI)

---

## 6) 데이터 & API 바인딩

- **API 호출**:
  - `GET /api/error/404` (정적 컨텐츠, 200 OK)
- **GA4 이벤트**:
  - `event_name: error_404`
  - `params: {error_type: "404", source: "banner_click"}`
- **캐시 정책**:
  - 오류 시 캐시 사용 불가 (정적 UI)

---

## 7) 검증 항목

1. "준비 중" 메시지가 중앙 정렬로 표시됨
2. "홈으로 돌아가기" 버튼 클릭 시 `HOME_001` 화면으로 이동
3. GA4 이벤트 `error_404`가 정상적으로 기록됨
4. 터치 타겟 크기가 44x44px 이상 보장됨

---

## 8) 향후 계획

- **2024 Q3**: 404 UI 디자인 개선 (이미지/애니메이션 추가)
- **GA4 이벤트 분석**: 404 발생 주요 경로 분석 및 개선

---

이 문서는 404 오류 시 사용자 경험 유지 및 분석을 위해 설계되었습니다. 모든 정책과 기술 스펙은 테스트 항목으로 검증 완료됨.
