// section.types.ts
import type {
  FlexDirection,
  FlexWrap,
  Justify,
  AlignItems,
  HeightMode,
} from '../../types/common.types';

// 공통 타입 re-export (기존 코드 호환성 유지)
export type { FlexDirection, FlexWrap, Justify, AlignItems, HeightMode };

// 컴포넌트별 고유 타입만 여기에 정의
