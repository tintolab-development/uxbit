{
  "name": "uxbit-monorepo",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@9.15.4",
  "scripts": {
    "dev": "pnpm --filter @uxbit/stencil-components run build && turbo run dev --parallel",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,md,css,scss,html}\"",
    "test": "turbo run test",
    "stencil:build": "pnpm --filter @uxbit/stencil-components run build",
    "stencil:component": "pnpm --filter stencil-components run serve",
    "storybook:local": "pnpm --filter storybook run dev",
    "storybook": "pnpm --filter @uxbit/stencil-components run build && turbo run dev --parallel --filter=@uxbit/stencil-components --filter=storybook",
    "storybook:build": "turbo run build --filter=storybook",
    "storybook:build:gh-pages": "pnpm --filter storybook run build:gh-pages",
    "templates": "pnpm --filter @uxbit/stencil-components run build && turbo run dev --parallel --filter=@uxbit/stencil-components --filter=templates",
    "mcp:build": "pnpm --filter @uxbit/stencil-components run build && pnpm --filter @uxbit/mcp-server run build",
    "mcp:start": "pnpm --filter @uxbit/mcp-server run start",
    "mcp:dev": "pnpm --filter @uxbit/stencil-components run build && pnpm --filter @uxbit/mcp-server run dev",
    "lint-staged": "lint-staged",
    "clean": "turbo run clean",
    "prepare": "husky install"
  },
  "devDependencies": {
    "@types/node": "^22.13.5",
    "eslint": "^9.39.1",
    "eslint-config-prettier": "^10.1.8",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.6",
    "prettier": "^3.6.2",
    "turbo": "^2.3.0",
    "typescript": "^5.9.3"
  },
  "lint-staged": {
    "*.{ts,tsx,js,jsx}": [
      "eslint --fix",
      "prettier --write"
    ],
    "*.{md,json,css,scss,html}": [
      "prettier --write"
    ]
  }
}
