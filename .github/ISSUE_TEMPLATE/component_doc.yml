name: '📘 Component Doc / 가이드'
description: '웹 컴포넌트 문서화/가이드 및 품질 평가'
title: '[Docs] <component>: <short summary>'
labels: ['type:docs', 'area:components', 'priority:normal']
assignees:
  - leezer94
body:
  - type: input
    id: component
    attributes:
      label: Component Name
      placeholder: e.g., tinto-[element]
    validations:
      required: true
  - type: input
    id: version
    attributes:
      label: Component Version
      placeholder: e.g., 1.0.0
    validations:
      required: true
  - type: textarea
    id: quality_score
    attributes:
      label: 품질 평가 결과 (MCP 서버 평가 도구 사용)
      description: 'MCP 서버의 evaluate_component_quality 도구로 평가한 결과를 붙여넣기'
      placeholder: |
        {
          "component": "tinto-button",
          "totalScore": 88.5,
          "grade": "A",
          "scores": {
            "consistency": 92,
            "reusability": 85,
            "completeness": 88,
            "performance": 85,
            "usability": 87,
            "standards": 100
          },
          "improvements": ["개선 제안 1", "개선 제안 2"]
        }
  - type: textarea
    id: overview
    attributes:
      label: Overview (KOR/EN)
      description: 간략 개요 + 목적(사용 상황), Shadow DOM/parts/slots 등
      placeholder: TL;DR + key features...
    validations:
      required: true
  - type: textarea
    id: props
    attributes:
      label: Props (표/리스트)
      description: 타입, reflect, default, 설명
      placeholder: direction, wrap, justify, align, gap, ... (+ *Desktop variants*)
  - type: textarea
    id: cssvars
    attributes:
      label: CSS Vars (Theming)
      description: --t-* 목록, 기본값, desktop 오버라이드 규칙
  - type: textarea
    id: a11y
    attributes:
      label: A11y
      description: role/aria 패스스루, scrollable tabIndex, focus behavior
  - type: textarea
    id: examples
    attributes:
      label: Usage Examples
      description: 모바일=데스크톱 동일, 데스크톱 전용 오버라이드, heightMode, center 등
  - type: textarea
    id: troubleshooting
    attributes:
      label: Troubleshooting
      description: 1920px에서 레이아웃 변동, 전역 ::part/root/변수 충돌 등
  - type: textarea
    id: acceptance
    attributes:
      label: Acceptance Criteria
      description: 완료 조건(체크리스트)
      placeholder: |
        - [ ] MCP 서버 품질 평가 완료 (A 등급 이상)
        - [ ] README/Wiki에 문서 반영
        - [ ] 컴포넌트별 테스트 코드 검증
        - [ ] CI 링크/배포 링크 업데이트
  - type: checkboxes
    id: check
    attributes:
      label: Pre-flight
      options:
        - label: 내용에 민감정보 없음
          required: true
        - label: 스크린샷/코드 스니펫 포함 (선택)
